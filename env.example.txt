# Spasht — environment variables
# Add these in Vercel: Project → Settings → Environment Variables
# Configure at least one analysis provider. Keys that are blank are skipped.

# Primary — fastest OCR and explanation route; free tier, 1,500 requests/day, no card
# https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Fallback 1 — Qwen 3.6 27B through Groq (explanation only; low per-minute token cap)
# https://console.groq.com/keys
GROQ_API_KEY=

# Fallback 2 — stage-specific OCR and explanation through DeepInfra
# Qwen3-VL-30B-A3B reads medical findings; Qwen3.5 models explain them.
# Also required locally by `npm run review:translations`
# https://deepinfra.com/dash/api_keys
DEEPINFRA_API_KEY=

# Optional natural narration — both values are required together
# Create a Speech resource in Azure AI services
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=
